## 1. Implementation
- [ ] Add migration to `profiles` for `first_name` and `last_name`
- [ ] Update `/api/user/profile` to return `first_name` and `last_name`
- [ ] Update `/api/user/profile/update` to write `first_name` and `last_name`
- [ ] Update talent profile UI mapping to prefer `first_name`/`last_name` and fallback to `full_name`
- [ ] Validate profile save/read flow with existing `full_name` data

## 2. Validation
- [ ] Run lint/test if available
- [ ] Manually verify profile edit/save in `/talent/profile`
